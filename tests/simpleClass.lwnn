class Main {
  def Main(a:A) = {
    a := new A();
    a.set(2, 4);
    print a.a;
    print a.b;
  }
}

class A {
  fields a : int, b: int;

  def set(x: int, y: int) = {
    self.a := x;
    self.b := y;
  }
}
